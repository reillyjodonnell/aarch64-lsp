{
  "name": "aarch64-lsp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "server": "bun --hot run src/server.ts --stdio",
    "test": "bun test",
    "test:hot": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  }
}
